# ADaM Builder 管理层简报

## 1. 项目一句话概述

ADaM Builder 是一个基于 R Shiny 和大语言模型的内部工作台，用于把 SDTM 数据和 Analysis Spec 快速转换为可审阅、可执行、可下载的 ADaM 结果。

## 2. 它解决什么问题

当前 ADaM 生成通常存在三类问题：

- 规格理解和代码编写依赖人工，耗时长
- 不同项目的 Spec 格式不统一，前期整理成本高
- AI 直接生成代码存在黑盒风险，难以审查和落地

本系统的目标是把“上传文件、解析规格、AI 生成代码、人工审核、输出结果”放进一个统一流程中。

## 3. 系统做了什么

系统当前可以完成以下工作：

- 上传多个 SDTM 域 CSV 文件
- 上传多个 Spec CSV 文件并自动识别列结构
- 自动汇总数据摘要并调用 LLM 生成 R 代码
- 自动输出风险提示，标注模型做出的假设
- 支持人工修改代码后再执行
- 动态展示并下载最终 ADaM 数据集

## 4. 核心价值

从管理视角，项目价值主要体现在四点：

- 提升效率  
  把原本分散的规格理解、代码生成和结果查看整合为一个工作流。

- 降低门槛  
  用户无需先手工整理严格 JSON 格式 Spec，系统可直接处理 CSV 规格表。

- 提高可控性  
  AI 输出必须附带风险日志，且代码执行前必须经过人工审阅。

- 保留扩展性  
  系统支持多模型提供商，也支持本地推理方式，便于后续适配不同部署策略。

## 5. 系统当前架构特点

系统整体可以理解为三段：

1. 输入层  
   接收 SDTM 与 Spec 文件，并做结构识别。

2. 生成层  
   把 Spec 与数据摘要送入 AI，生成代码和风险说明。

3. 输出层  
   让用户审查代码、执行结果，并查看生成的数据集。

它不是一个单纯的“聊天式 AI”，而是一个有明确输入、明确输出、明确审核点的业务工具。

## 6. 技术亮点

- 配置驱动  
  支持的 SDTM 域和模型提供商都通过注册表管理，后续扩展成本较低。

- 结构化 AI 输出  
  模型必须返回固定 JSON，包括代码和风险日志，便于系统接收和展示。

- 审核闭环  
  从 Spec 解析到代码执行都有人工确认环节，降低误用风险。

- 多数据集支持  
  输出结果不是写死的，可按目标数据集名称动态生成和展示。

## 7. 当前边界

从现有代码静态分析看，当前系统更适合：

- 内部演示
- 原型验证
- 辅助研发
- 规则较明确的 ADaM 构建场景

当前仍需关注的问题包括：

- 动态代码执行的安全边界
- 缺少自动化测试材料
- 缺少持久化审计和任务留痕机制
- 更复杂规则下的生成稳定性仍需业务验证

## 8. 建议的后续方向

建议后续演进聚焦四个方向：

- 增加测试与验证机制
- 增加审计日志与结果留痕
- 加强执行环境隔离
- 沉淀高频 ADaM 规则模板，减少对 Prompt 的单次依赖

## 9. 管理层可直接关注的结论

ADaM Builder 已经具备一个较完整的 AI 辅助数据编程闭环雏形。  
它的优势不是“完全替代人工”，而是“让 AI 生成进入可控、可审查、可落地的工作流”。  
如果后续补齐测试、安全和审计能力，该系统具备进一步产品化或平台化演进的基础。
